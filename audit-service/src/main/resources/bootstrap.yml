spring:
  application:
    name: audit-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
      retry:
        max-attempts: 3
        initial-interval: 1000
